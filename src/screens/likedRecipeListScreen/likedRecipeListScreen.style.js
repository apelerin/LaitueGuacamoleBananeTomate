import {StyleSheet} from 'react-native';

import {lavenderBlush} from '../../../assets/colors';

const style = StyleSheet.create({
  mainContainer: {
    flex: 1,
    alignItems: 'center',
    backgroundColor: lavenderBlush,
  },
});

export default style;
